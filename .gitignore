node_modules
dist
.DS_Store
*.local
.vite
.vercel

# Source 3D models — large binaries, and the web build never reads them.
# Kept on disk under model/ as the render source for the frame sequences.
model/*.glb

# Scratch output from the render and audit scripts.
scripts/.tmp-frames
scripts/.preview
scripts/.shots
scripts/.*.json
scripts/.*.mjs
scripts/.*.png
